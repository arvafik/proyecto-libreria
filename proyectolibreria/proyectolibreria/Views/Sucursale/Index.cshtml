@model List<proyectolibreria.Models.ViewModels.ListSucursaleViewModel>
@{
    ViewBag.Title = "Sucursales";
}


<div class="row">
    <div class="col">
        <div class="card">
            <!-- Card -->
            <div class="card-header border-0">
                <h3 class="mb-0">@ViewBag.Title</h3>
                <div class="col text-right">
                    <a href="~/Sucursale/Nuevo" class="btn btn-sm btn-primary">Agregar sucursal</a>
                </div>
            </div>
            <!-- tabla -->
            <div class="table-responsive">
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Direccion</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody class="list">
                        @foreach (var oElemento in Model)
                        {
                            <tr>
                                <td>@oElemento.ID</td>
                                <td>@oElemento.Nombre</td>
                                <td>@oElemento.Direccion</td>
                                <td>
                                    <a class="btn btn-default" href="~/Sucursale/Editar/@oElemento.ID">Editar</a>
                                    <a class="btn btn-danger" href="~/Sucursale/Eliminar/@oElemento.ID">Eliminar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
